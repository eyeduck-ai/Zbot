/**
 * Frontend Configuration
 * 
 * 集中管理前端所有可調整的設定參數。
 * 修改這裡的值會影響整個前端應用程式的行為。
 */

// =============================================================================
// Demo 模式設定 (錄影用)
// =============================================================================

/** Demo 模式 - true: 使用模擬資料 / false: 連接真實後端 */
export const DEMO_MODE = false;  // ← 錄影時改成 true

// =============================================================================
// 閒置自動登出設定
// =============================================================================

/** 閒置警告觸發時間 (毫秒) - 預設 1.5 分鐘 */
export const IDLE_TIMEOUT_MS = 1.5 * 60 * 1000;

/** 登出倒數秒數 - 預設 30 秒 */
export const IDLE_COUNTDOWN_SECONDS = 30;

// =============================================================================
// 任務輪詢設定
// =============================================================================

/** 任務輪詢間隔 (毫秒) - 預設 1.5 秒 */
export const TASK_POLL_INTERVAL_MS = 1500;

/** 任務最大輪詢次數 - 預設 60 次 */
export const TASK_POLL_MAX_ATTEMPTS = 60;

// =============================================================================
// 背景任務指示器設定
// =============================================================================

/** 有執行中任務時的輪詢間隔 (毫秒) */
export const BG_TASKS_POLL_RUNNING_MS = 3000;

/** 無執行中任務時的輪詢間隔 (毫秒) */
export const BG_TASKS_POLL_IDLE_MS = 30000;

// =============================================================================
// API 設定
// =============================================================================

/** API 請求超時時間 (毫秒) - 預設 30 秒 */
export const API_TIMEOUT_MS = 30000;
